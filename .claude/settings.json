{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -e '.tool_input.command | test(\"^\\\\s*git\\\\s+commit(\\\\s|$)\")' >/dev/null && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"CLAUDE.md forbids git commits from the agent.\"}}' || true"
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | grep -qE '(^|/)crates/proto/src/generated/' && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"crates/proto/src/generated/ is regenerated by `just proto` from proto/ledger/v1/*.proto. Hand-edits are silently overwritten. Edit the .proto source and run `just proto` instead.\"}}' || true"
          },
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // \"\"' | grep -qE '(^|/)Cargo\\.lock$' && echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Cargo.lock is generated by cargo. Hand-edits cause drift that CI rejects. Update Cargo.toml and run `cargo +1.92 build --workspace` (or `cargo update -p <crate>`) to regenerate.\"}}' || true"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATH\" == *.rs ]]; then cargo +nightly fmt -- \"$CLAUDE_FILE_PATH\" 2>/dev/null; fi"
          },
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATH\" == *.rs && \"$CLAUDE_FILE_PATH\" == *crates/* ]]; then crate=$(echo \"$CLAUDE_FILE_PATH\" | sed -n 's|.*crates/\\([^/]*\\)/.*|\\1|p'); if [[ -n \"$crate\" && -f \"crates/$crate/Cargo.toml\" ]]; then pkg=$(awk -F'\"' '/^name =/ {print $2; exit}' \"crates/$crate/Cargo.toml\"); if [[ -n \"$pkg\" ]]; then out=$(timeout 20 cargo +1.92 check -p \"$pkg\" --lib --tests --message-format=short --quiet 2>&1); if [[ $? -ne 0 ]]; then echo \"[cargo check -p $pkg] errors:\"; echo \"$out\" | grep -E '^(error)' | head -15; fi; fi; fi; fi"
          },
          {
            "type": "command",
            "command": "if [[ \"$CLAUDE_FILE_PATH\" == *.proto ]]; then echo \"[reminder] .proto changed — run \\`just proto\\` to regenerate crates/proto/src/generated/ledger.v1.rs, then \\`just fmt\\` and \\`just clippy\\`.\"; fi"
          },
          {
            "type": "command",
            "command": "f=\"$CLAUDE_FILE_PATH\"; [[ -z \"$f\" || ! -f \"$f\" ]] && exit 0; if [[ \"$f\" == *.rs || \"$f\" == *.md ]]; then grep -qE '^[[:space:]/]*```ignore\\b' \"$f\" && echo \"[writing-check] fenced 'ignore' blocks are banned. Use 'no_run' for Rust examples (validated by cargo doc) or 'text' for non-Rust content.\"; fi; if [[ \"$f\" == *.md ]]; then bad=$(awk 'BEGIN{i=0;b=0} /^[[:space:]]*```/{if(i){i=0}else{if($0~/^[[:space:]]*```[[:space:]]*$/)b++;i=1}next} END{print b+0}' \"$f\"); [[ \"$bad\" -gt 0 ]] && echo \"[writing-check] $bad untagged code-fence opener(s) — language-tag every opener (e.g. rust, bash, text).\"; base=$(basename \"$f\" .md); case \"$base\" in CLAUDE|AGENTS|README|MEMORY|LICENSE*|CONTRIBUTING|CHANGELOG|SECURITY|CODEOWNERS|CODE_OF_CONDUCT|SPEC|DESIGN|WHITEPAPER|PII|PRD|NOTICE|TODO|SKILL) ;; *) [[ \"$base\" =~ ^[a-z0-9]+(-[a-z0-9]+)*$ ]] || echo \"[writing-check] '$base.md' is not kebab-case (lowercase letters/digits separated by hyphens).\";; esac; fi; exit 0"
          },
          {
            "type": "agent",
            "prompt": "`crates/state/src/system/keys.rs` was just edited in the InferaDB Ledger workspace. Read `.claude/agents/data-residency-auditor.md` for the full audit checklist, then apply it to the current state of `crates/state/src/system/keys.rs` and any files it references. This is read-only verification — produce findings in the auditor's standard format (BLOCK / FIX / NOTE per finding, counts per severity, one-line verdict). Do not edit any file.",
            "if": "Edit(crates/state/src/system/keys.rs)",
            "timeout": 180
          },
          {
            "type": "agent",
            "prompt": "`crates/state/src/system/keys.rs` was just written in the InferaDB Ledger workspace. Read `.claude/agents/data-residency-auditor.md` for the full audit checklist, then apply it to the current state of `crates/state/src/system/keys.rs` and any files it references. This is read-only verification — produce findings in the auditor's standard format (BLOCK / FIX / NOTE per finding, counts per severity, one-line verdict). Do not edit any file.",
            "if": "Write(crates/state/src/system/keys.rs)",
            "timeout": 180
          },
          {
            "type": "agent",
            "prompt": "A file under `crates/server/tests/` was just edited in the InferaDB Ledger workspace. The hook input JSON is:\n\n$ARGUMENTS\n\nExtract `tool_input.file_path` from that JSON. Read `.claude/agents/test-isolation-auditor.md` for the full audit checklist, then apply it to the edited file and the surrounding `crates/server/tests/` directory. Focus on: `use crate::common::` vs `mod common;`, `autotests = false` + single `[[test]]` in Cargo.toml, port allocation via `allocate_ports`, `TestCluster` teardown, required clippy allows at file top, `mod X;` registration in `tests/integration.rs`. Read-only — produce findings in the auditor's standard format. Do not edit.",
            "if": "Edit(crates/server/tests/**)",
            "timeout": 180
          },
          {
            "type": "agent",
            "prompt": "A file under `crates/server/tests/` was just written in the InferaDB Ledger workspace. The hook input JSON is:\n\n$ARGUMENTS\n\nExtract `tool_input.file_path` from that JSON. Read `.claude/agents/test-isolation-auditor.md` for the full audit checklist, then apply it to the written file and the surrounding `crates/server/tests/` directory. Focus on: `use crate::common::` vs `mod common;`, `autotests = false` + single `[[test]]` in Cargo.toml, port allocation via `allocate_ports`, `TestCluster` teardown, required clippy allows at file top, `mod X;` registration in `tests/integration.rs`. Read-only — produce findings in the auditor's standard format. Do not edit.",
            "if": "Write(crates/server/tests/**)",
            "timeout": 180
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\":{\"hookEventName\":\"SessionStart\",\"additionalContext\":\"Serena MCP is available for this Rust workspace. Call mcp__plugin_serena_serena__activate_project with project=/Users/evan/Developer/inferadb/ledger once at the start of any code navigation or editing work — it enables symbolic tools (find_symbol, replace_symbol_body, get_symbols_overview, find_referencing_symbols) that the project CLAUDE.md relies on for safe edits.\"}}'"
          }
        ]
      }
    ]
  }
}
